<h2>New Category</h2>

<%= form_for @category do |f| %>

  <% if @category.errors.any? %>
    <div id="errors">
      <h2><%= pluralize(@categor.errors.count, "error") %>
              prevented this category from saving</h2>
      <ul>
        <% @category.errors.full_messages.each do |msg| %>
          <li> <%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :name  %>
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end  %>